import { MaskPointDirective } from './mask-point.directive';

describe('MaskPointDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskPointDirective();
    expect(directive).toBeTruthy();
  });
});
